//use std::fs;
//
//use elf_parser::parser::header::parse_elf_magic_number;

fn main() -> Result<(), Box<dyn std::error::Error>> {
    //let raw = fs::read("main")?;
    //let raw = raw.as_slice();
    //let result = parse_elf_magic_number(raw)?;
    //dbg!(result);
    Ok(())
}
